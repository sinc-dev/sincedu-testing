{
  "name": "sincedu-testing-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright end-to-end tests for the SINC EDU testing dashboard. Isolated from the pnpm workspace on purpose (own npm-managed node_modules).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
